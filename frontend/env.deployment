# 部署包构建时由 deploy/prepare 复制为 .env.production.local，供 Vite 注入生产环境变量。
# 默认可留空或保留 127.0.0.1：系统会优先使用浏览器当前访问地址生成签字二维码。
# 例如用户用 http://10.2.254.29:3000 打开系统，二维码也会自动使用该地址。
# 仅当你需要固定域名/反向代理地址时，改成 http://服务器IP:端口 或 https://域名。
VITE_PUBLIC_BASE_URL=http://127.0.0.1:3000
