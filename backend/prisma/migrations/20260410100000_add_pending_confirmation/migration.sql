-- SQLite does not enforce enum values at the database level.
-- The new 'pending_confirmation' status is handled at the application layer.
-- This migration is a no-op placeholder to keep the migration history in sync.
SELECT 1;
