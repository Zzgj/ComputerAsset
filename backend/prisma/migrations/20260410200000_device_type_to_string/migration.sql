-- SQLite stores enum values as plain text strings.
-- Changing the Prisma type from DeviceType enum to String requires no DDL change.
-- This migration is a placeholder to keep the migration history in sync.
SELECT 1;
